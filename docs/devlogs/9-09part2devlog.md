# 9-09 part2 devlog — pet 极简重构日（P201~P205）

> 背景：P200 板上异常（待机变来变去+点击大小跳变）未闭环；用户连下三轮需求：
> ①新图直连+旧对接删除 ②三层架构重构 ③命名卡死/喂食简化/Settings对接
> ④点按只触摸 ⑤狗不做/三连点喂食/砍健康联动/三开关。全部编译+打包+资源检查+固化。
> 对应 devlog.md 节点表 9-9 P201~P205。P200 板上验证仍待上板（代码侧根因已在 P201 处理）。

## P201（ok-20260909-3）：cat_sprites 直连 + 原生 12 帧

- 源=`/data/dm/catimg/cat_sprites/` 12态×12帧=144张128×128 PNG；旧 `luncher_dm/` 192个C（每态16帧，12~15系0~3复制凑数）全删。
- `gen_lvgl_sprites.py` 重写：Linux路径+原生12帧+同步生成 `pet_sprites.h`/`pet_frame_table.c`+patch Makefile（192→144）。
- `dm_pet.h`：`PET_FRAMES_PER_STATE`/`PET_WALK_FRAMES` 16→12。
- P200根因处理（代码侧）：①行为切换硬切frames[0]→不断帧（跳变主因）②复制帧回放消除
  ③呼吸2.6Hz±1.5%→0.7Hz±0.8% ④弹跳±10/16→±4/6（P199音乐控件误判推手）。
- 动画双计数器独立推进，每3tick切一帧（≈2.8fps，对齐spec 2-4fps）；walk/state帧索引解耦。
- 构建污染清理（P190重演防范）：两 `libapps.a` 192旧成员全删+盘上陈旧.o，重编后144成员、`nuttx.elf` 288符号、零12-15残留。
- 固件17.6MB（128原生9MB精灵 vs 旧300px；9MB系固件大头，见P205备注）。

## P202（ok-20260909-4）：pet 三层重构（public API零改动）

- `dm_pet.c` 1900行god file→门面230行；新 `pet_core.h/c`（引擎：属性/状态机/行为/事件/投喂/持久化，零lv_obj）
  + `pet_view.h/c`（视图：全部LVGL/输入/动画/弹窗）；core→view只经 `pet_view_ops_t` 回调表。
- 行为层有名无形→行为配帧窗（IDLE 5行为各切2~3帧等），view检测窗变化从窗首重播；EAT/PLAY整幅。
- 持久化load改cJSON（`CONFIG_NETUTILS_CJSON=y`，链上已有；emit保持原格式兼容旧档）。
- 事件队列保留（线程边界：语音pet:命令虽经LVGL timer分发，但队列是跨线程安全网；删poll直调方案被调用链否决），硬化64深掩码+溢出计数。
- 睡眠唤醒timer resume系死代码（timer只在主屏WAKE_UP路径暂停，彼时无互动入口）→删。
- 新增 `dm_pet_set_roam_rect()`（暂无调用者被--gc-sections裁，待ui_home接线）。
- WAKE_UP/SLEEP改门面同步处理（timer暂停/恢复+停靠即时生效），core不再收。

## P203（ok-20260909-5）：命名卡死根因 + Settings对接 + 喂食简化

- 卡死根因：起名overlay无确定/取消、无关闭路径；键盘READY回调只删keyboard不删overlay→黑遮罩永久吞点击；
  CANCEL啥都不干；连点叠两层遮罩即假卡死。修：白卡+标题+确定/取消（WiFi同款）+重入guard+空名按取消；
  名字≤15字→≤10字（name[32]装不下15汉字，旧静默截断）。
- Settings宠物区：名字行value误传pet_type（显示碰巧对）→传名字；新增种类（只读）/成长阶段/状态行（饿→饱=100-hunger）。
- 喂食弹窗：砍属性五维（搬Settings）+4食物→鱼/零食两大按钮（零食未成年置灰+成年解锁注；肉/菜留引擎枚举）。

## P204（ok-20260909-6）：点按语义（短按只触摸，长按出菜单）

- 短按只做触摸反应不弹条；长按抚摸+弹互动条。后被P205覆盖（互动条删除，长按只抚摸）。

## P205（ok-20260909-7）：极简 + 三开关（用户拍板：狗不做/健康联动不做/喂食保持）

- 三连点直开喂食弹窗（喂食难触发，菜单已删不绕路）；长按只抚摸；互动条整体删除（-200行）。
- 删健康联动5事件（枚举+core分支+ui_home 5埋点；喝水/久坐只干自己事）；音乐/天气/语音保留。
- 删狗：`PET_TYPE_DOG`、`set_type/get_type/refresh_sprite`、狗饥饿速率、狗/肉显示分支；`pet_type`字段保留恒0免迁移。
- Settings三件套：宠物显示总开关（关立即拆UI+停timer，下次锁屏不建）、饥饿提醒开关（关只留饿行为+秒撤已弹红泡）、
  重新开始（红字二次确认，名字/成长/属性清零、开关保持，三行显示同步刷）。
- 存档v2（+enabled/hunger_alert；v1老档照读缺项默认开）。⚠️回滚注意：旧固件读不懂v2会重置宠物，回滚前备份`/data/pet_state.json`。
- CELEBRATE/GROOM现无触发源（休眠保留，帧是art无浪费，池表~1KB未动——用户原话“逻辑再丰富也没用”故只冻不删）。
- 固件瘦身诚实话：本轮删的全是代码（KB级）；大头是9MB精灵，真要瘦得128重切96（省~4MB，需art重工+验视觉，本次未做）。

## 待上板（新会话第一件事）

P200板上异常是否消除（待机形态/点击大小）+ P201~P205回归（命名三路关闭/三连点喂食/长按抚摸/三开关/关总开关锁屏无猫/断电多次）+ 反复断电确认P198修複。
