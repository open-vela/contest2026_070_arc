# 9-10 part3 devlog — P209：WiFi包自证 + 日历导航返工（ok-39~41）

> 日期：2026-09-10 ｜ 节点：P209 ✅ ｜ 基线 ok-20260910-41

## 一、WiFi“包问题”自证（ok-39/40）
- 用户质疑 pack 没打进 WiFi 配置。实查：`usrdata.fex` 内完整 `wifi-home` 配置躺着（跨行正则首查误报，已用二进制抠出全文验证）。
- `lunch_nuttx 2` = r528s3-gemini-s1（亲测菜单），vela.bin == nsh.fex（md5 一致）。
- distclean 全量重编 + pack（ok-40）。结论：包没问题，板上连不上走 `cat /data/etc/wifi/wapi.conf` 定界（种子到没到板）。

## 二、日历导航返工（ok-41）
- 用户板上翻不了月：箭头圆钮（P206b 全透明→OPA_40 半透仍难找）是根因。
- 改：标题独立大行“2026 年 9 月”居中；翻月换“上个月/今天/下个月”文字药丸；
  周首行全称“星期日~星期六”。
- 另：`show_subpage` 内容区居中致超高吃标题，已改顶对齐（ok-38）；行分隔线半透明。

## 待办
1. 烧 ok-41：日历翻月/标题/周行确认。
2. WiFi：板上 `cat` 定界。
3. 脱敏 push → fork 重搬 → PR+CLA；视频 + 报告收尾。
